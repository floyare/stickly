const BackgroundTip = () => {
    return (
        <div className="background__tip">
            <p><kbd>Ctrl</kbd> + <kbd>Z</kbd> to undo.</p>
            <p><kbd>Ctrl</kbd> + <kbd>Y</kbd> to redo.</p>
        </div>
    );
}
 
export default BackgroundTip;
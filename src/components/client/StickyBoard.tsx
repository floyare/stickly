import Styles from "../../styles/StickyBoard.module.scss"

const StickyBoard = () => {
    return (
        <section className={Styles.sticky__board}>
            
        </section>
    );
}

export default StickyBoard;